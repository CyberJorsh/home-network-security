{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local application window; no frontend shell, filesystem or arbitrary network access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
